services:
  octopus-cache:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - octopus_data:/data
    environment:
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  octopus_data: